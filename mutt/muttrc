set folder = "imaps://me@mail.wantyapps.xyz:993"
set smtp_url = "smtp://me@mail.wantyapps.xyz:587"

set from = "me@wantyapps.xyz"
set realname = "Uri Arev"
set signature = "sig"

set spoolfile = "+INBOX"
set record = "+Sent"
set trash = "+Trash"
set postponed = "+Drafts"

mailboxes =INBOX =Sent =Trash =Drafts =Junk

source color.muttrc
source dracula.muttrc

bind pager j next-line
bind pager k previous-line
# bind index,pager § exec pipe-message ./scripts/edit_email.sh

# unset sig_dashes
set sort = reverse-date-received
set signature = "$HOME/.config/mutt/sig"
